name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          check-latest: true

      - name: Run Tests
        run: go test -v -race ./...

      - name: Build Binaries
        run: |
          VERSION="${{ github.ref_name }}"
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-s -w -X main.AppVersion=${VERSION}" -o tgdown-linux-amd64 .
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -ldflags="-s -w -X main.AppVersion=${VERSION}" -o tgdown-linux-arm64 .
          tar -czf tgdown-linux-amd64.tar.gz tgdown-linux-amd64
          tar -czf tgdown-linux-arm64.tar.gz tgdown-linux-arm64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            tgdown-linux-amd64
            tgdown-linux-arm64
            tgdown-linux-amd64.tar.gz
            tgdown-linux-arm64.tar.gz
          generate_release_notes: true
